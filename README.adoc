= SithTermFX
:toc: macro
:toc-title: Table of Contents
:toclevels: 3
:icons: font

SithTermFX is a terminal emulator for JavaFX.
The project is a result of porting https://github.com/JetBrains/jediterm[JediTerm] (commit 8366f2b) with new features.
Therefore, the Terminal Emulator based on this library can be seamlessly integrated into any JavaFX application.
A detailed comparison of terminal libraries is provided below.

toc::[]

== Features

* Local terminal for Unix, Mac and Windows using Pty4J
* *21 terminal emulations* — XTerm, DEC VT100–VT520, IBM TN3270/TN5250, Wyse 50/60/160, TVI 910/920/925, HP 2392/700-92, SCO ANSI, Sun CDE, DEC CTERM, Commodore PETSCII
* Xterm 256 colours and true-color support
* Full TN3270E (RFC 2355) and TN5250E (RFC 4777) protocol with EBCDIC, block-mode fields, structured fields, and AID keys
* Scrolling
* Copy/Paste
* Mouse support
* Terminal resizing from client or server side
* Terminal tabs
* *Dynamic font size* — Adjust font size at runtime via Ctrl+Plus/Minus (Cmd+Plus/Minus on Mac) without disconnecting
* *Split screen* — Nested horizontal/vertical splits with independent sessions per pane
* *Reorder split panes* — Drag a split terminal onto another to move it above, below, left, or right; drop zones show possible positions while dragging

== Terminal Comparison

[cols="1,1,1,1",options="header"]
|===
| Terminal        | SithTermFX | https://github.com/JetBrains/jediterm[JediTerm] | https://github.com/javaterminal/TerminalFX[TerminalFX]
| GUI Library     | JavaFX     | Swing          | JavaFX
| Main Component  | Canvas     | JComponent     | WebView
| Languages       | Java       | Java, Kotlin   | Java, JS
| JPMS Support    | Yes        | No             | Yes
|===

== Downloads

Release artifacts (JARs and application tarball) are published on the link:https://github.com/chardonnay/SithTermFX/releases[GitHub Releases] page.
*No authentication is required* — anyone can download them.

* Latest release: link:https://github.com/chardonnay/SithTermFX/releases/latest[Releases → Latest]
* Direct download URLs (replace `VERSION` with e.g. `1.1.0`):
** Application JAR: `https://github.com/chardonnay/SithTermFX/releases/download/vVERSION/sithtermfx-app-VERSION.jar`
** Core library: `sithtermfx-core-VERSION.jar`, UI library: `sithtermfx-ui-VERSION.jar`
** Standalone app tarball: `sithtermfx-app-VERSION.tar`

These assets are attached to each release by the CI workflow; they are not published to GitHub Packages, so no login or token is needed.

== Usage

It is recommended to start working with SithTermFX by studying and running the
link:sithtermfx-app/src/main/java/com/sithtermfx/app/example/BasicTerminalShellExample.java[BasicTerminalShellExample] class.
This class contains the minimal code needed to launch a terminal in a JavaFX application.

=== Dependencies

To use SithTermFX as a library, add the dependencies below.
Release artifacts are available from the link:https://github.com/chardonnay/SithTermFX/releases[GitHub Releases] page (see <<Downloads>>) — no login required.
Until Maven Central publication is available, either download the JARs from Releases or build from source as described below.

[source,xml]
----
<dependency>
    <groupId>com.sithtermfx</groupId>
    <artifactId>sithtermfx-core</artifactId>
    <version>${sithtermfx.version}</version>
</dependency>
<dependency>
    <groupId>com.sithtermfx</groupId>
    <artifactId>sithtermfx-ui</artifactId>
    <version>${sithtermfx.version}</version>
</dependency>
----

=== Dark Theme

If you need a dark theme, you should override the `getDefaultForeground()` and `getDefaultBackground()` methods in
`UserSettingsProvider`.
To run the demo application in dark theme see <<application-maven>>.

=== Dynamic Font Size

Use `DynamicFontSizeSettingsProvider` (or extend it) to enable runtime font resizing without disconnecting the terminal:

* *Ctrl+Plus* / *Ctrl+Minus* (Cmd on Mac) — Increase/decrease font size
* *Ctrl+0* (Cmd+0 on Mac) — Reset to default size

Font size range is configurable via `getMinFontSize()` and `getMaxFontSize()` in `UserSettingsProvider` (default 8–72 pt).

=== Split Screen

Use `TerminalSplitPane` for nested horizontal/vertical splits.
Right-click in the terminal to access the context menu:

* *Split right (same server)* / *Split right (new connection)* — Split horizontally
* *Split down (same server)* / *Split down (new connection)* — Split vertically
* *Close split* — Close the focused pane

*Reorder panes by drag-and-drop:* Drag a split terminal with the mouse onto another pane.
While dragging, four drop zones (above, below, left, right) are shown; the zone under the cursor is highlighted.
Release to move the pane to that position.

Implement `SplitConnectorFactory` to provide new `TtyConnector` instances.
The `SplitRequest` contains the chosen mode (SAME_SERVER_NEW_SHELL vs NEW_CONNECTION) and the parent widget for reusing connections.

See link:sithtermfx-app/src/main/java/com/sithtermfx/app/example/SplitTerminalShellExample.java[SplitTerminalShellExample] for a demo.

=== Terminal Emulations

SithTermFX supports 21 terminal emulation types out of the box.
Select an emulation by calling `setEmulationType()` on the widget *before* calling `start()`:

[source,java]
----
import com.sithtermfx.core.emulator.EmulationType;

widget.setEmulationType(EmulationType.VT220);
widget.setTtyConnector(connector);
widget.start();
----

The following table lists all available emulations:

[cols="2,2,1,1,1",options="header"]
|===
| EmulationType   | Display Name          | Default Size | Screen Mode | Color

| `XTERM`         | XTerm (256 color)     | 80×24        | Stream      | True Color
| `VT100`         | DEC VT100             | 80×24        | Stream      | Mono
| `VT102`         | DEC VT102             | 80×24        | Stream      | Mono
| `VT220`         | DEC VT220             | 80×24        | Stream      | Mono
| `VT320`         | DEC VT320             | 80×24        | Stream      | Mono
| `VT420`         | DEC VT420             | 80×24        | Stream      | Mono
| `VT520`         | DEC VT520             | 80×24        | Stream      | Mono
| `TN3270`        | IBM TN3270            | 80×24        | Block       | 16 Color
| `TN5250`        | IBM TN5250            | 80×24        | Block       | 16 Color
| `SUN_CDE`       | Sun CDE (dtterm)      | 80×24        | Stream      | 16 Color
| `WY50`          | Wyse 50               | 80×24        | Stream      | Mono
| `WY60`          | Wyse 60               | 80×24        | Stream      | Mono
| `WY160`         | Wyse 160              | 80×24        | Stream      | 16 Color
| `TVI910`        | TeleVideo 910         | 80×24        | Stream      | Mono
| `TVI920`        | TeleVideo 920         | 80×24        | Stream      | Mono
| `TVI925`        | TeleVideo 925         | 80×24        | Stream      | Mono
| `HP2392`        | HP 2392               | 80×24        | Stream      | Mono
| `HP700_92`      | HP 700/92             | 80×24        | Stream      | 16 Color
| `SCOANSI`       | SCO ANSI              | 80×25        | Stream      | 16 Color
| `CTERM`         | DEC CTERM             | 80×24        | Stream      | Mono
| `PETSCII`       | Commodore PETSCII     | 40×25        | Stream      | 16 Color
|===

For block-mode terminals (TN3270, TN5250) you must also use their dedicated `TtyConnector` implementations.
See the link:docs/integration-guide.adoc[Integration Guide] for detailed examples, including the
link:docs/integration-guide.adoc#_emulation_architecture_overview[Emulation Architecture] diagram.

=== Hyperlinks

SithTermFX provides a wide range of features when working with links.
The `HighlightMode` enumeration specifies multiple modes of working with links and their colors.
In the `ALWAYS` modes, links are always underlined and always clickable.
In the `NEVER` modes, links are never underlined and never clickable.
In the `HOVER` modes, links become underlined and clickable only when hovered over.
Now let's clarify the difference between the two types of colors.
`CUSTOM` colors are those set by the SithTermFX user in the `getHyperlinkColor()` method of the settings.
`ORIGINAL` colors are those offered by the program running in the terminal.
Thus, links can use either custom colors or the original text colors.

== Public API

Public API documentation for integrators is available in link:docs/public-api.adoc[docs/public-api.adoc].
It defines stable packages, recommended entry points, and compatibility rules for upgrades.

== Code Building

To build the library use standard Git and Maven commands:

[source,bash]
----
git clone https://github.com/chardonnay/SithTermFX.git
cd SithTermFX
mvn clean install
----

[[application]]
== Running the Application

The project contains a demo application that shows how to use this library.
There are two ways to run the application.

[[application-maven]]
=== Using Maven

Build the project from the root first (required so all modules are available on the classpath):

[source,bash]
----
git clone https://github.com/chardonnay/SithTermFX.git
cd SithTermFX
mvn clean install
----

Then run the main application:

[source,bash]
----
mvn javafx:run -pl sithtermfx-app
----

To run the *Split Terminal* demo (nested splits and drag-and-drop reordering):

[source,bash]
----
mvn javafx:run -pl sithtermfx-app -Psplit
----

Debugger settings are in `sithtermfx-app/pom.xml`.
To try a dark theme, uncomment the following line in the JavaFX plugin configuration:

[source,xml]
----
<!--<commandlineArgs>theme=dark</commandlineArgs>-->
----

This will make SithTermFX use the `DarkThemeSettingsProvider`.

=== Using Distro

After building the project, you will find a distribution archive named `sithtermfx-app-<version>.tar` in the
`sithtermfx-app/target` directory.
Extracting this file will allow you to launch the application using `.sh` or `.bat` scripts depending on your operating system.

== Integration Guide

A comprehensive guide for embedding SithTermFX into your own JavaFX application is available
in link:docs/integration-guide.adoc[docs/integration-guide.adoc].
It covers dependencies, minimal setup, SSH connectors, theming, dynamic font size, split terminals,
hyperlinks, and advanced patterns extracted from the https://github.com/chardonnay/korTTY[korTTY] SSH client.

== License

SithTermFX is licensed under the Apache License 2.0.
See `LICENSE-APACHE-2.0.txt`.

== Contributing

We welcome all contributions.
You can help by reporting bugs, suggesting improvements, or submitting pull requests with fixes and new features.
If you have any questions, feel free to reach out — we'll be happy to assist you.

== Support Us

You can support our open-source work through https://github.com/sponsors/chardonnay[GitHub Sponsors].
Your contribution helps us maintain projects, develop new features, and provide ongoing improvements.
Multiple sponsorship tiers are available, each offering different levels of recognition and benefits.
